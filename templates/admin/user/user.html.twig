{% extends 'base.html.twig' %}

{% block title %}User - {{ user.email }}{% endblock %}

{% block bread %}
-> User
{% endblock %}

{% block body %}
    {{ include('admin/user/_user.html.twig', {user: user}) }}
    <div class="mt-4">
    {% for user in users %}
        <div>
            <div class="flex items-center">
                <div class="inline-block bg-gray-200 rounded-full text-3xl w-8 h-8 ring-2 ring-white mr-2"></div>
            </div>
            <div class="flex">
                <div class="border border-gray-300 ml-4 mr-6 pb-12"></div>
            <div>{{ user.email }}</div>
            </div>
        </div>
    {% endfor %}
    </div>
{% endblock %}