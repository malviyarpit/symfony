{% extends 'base.html.twig' %}

{% block title %}Book - {{ book.title }}{% endblock %}

{% block bread %}
-> Book
{% endblock %}

{% block body %}
    {{ include('book/_book.html.twig', {book: book}) }}
    <div class="mt-4">
    {% for comment in book.comments %}
        <div>
            <div class="flex items-center">
                <div class="inline-block bg-gray-200 rounded-full text-3xl w-8 h-8 ring-2 ring-white mr-2"></div>
            {# <div>
                <div class="text-gray-800 font-medium">Author</div>
                <div class="text-gray-500 font-medium">When</div>
            </div> #}
            </div>
            <div class="flex">
            <div class="border border-gray-300 ml-4 mr-6 pb-12"></div>
            <div>{{ comment.text }}</div>
            </div>
        </div>
    {% endfor %}
    </div>
{% endblock %}